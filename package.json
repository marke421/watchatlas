{
  "name": "watchatlas",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "WatchAtlas — Curated Chinese microbrand watch finder",
  "engines": { "node": ">=20" },
  "scripts": {
    "dev":        "vercel dev",
    "add-watch":  "node scripts/add-watch.js"
  },
  "devDependencies": {
    "vercel": "^34.0.0"
  }
}
